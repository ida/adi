Introduction
============

Add '<div class=loader id="relative_url"></div>' 
to a template, replace relative_url with the desired
destination and the div will be filled
with the '#content'-area of the relative_url.
